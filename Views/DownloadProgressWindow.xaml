<Window x:Class="RenPyTRLauncher.Views.DownloadProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="İndirme" Height="150" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1A1A22">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtGameName" Text="Oyun İndiriliyor..." 
                   Foreground="White" FontSize="14" FontWeight="SemiBold" 
                   Margin="0,0,0,10"/>

        <ProgressBar x:Name="ProgressBar" Grid.Row="1" Height="25" 
                     Minimum="0" Maximum="100" Value="0"
                     Background="#2A2A35" Foreground="#3498DB"/>

        <TextBlock x:Name="TxtPercentage" Grid.Row="2" Text="0%" 
                   Foreground="#BDC3C7" FontSize="12" 
                   HorizontalAlignment="Center" Margin="0,10,0,10"/>

        <Button x:Name="BtnCancel" Grid.Row="3" Content="İptal" 
                Width="100" Height="30" 
                Background="#E74C3C" Foreground="White" 
                BorderThickness="0" Cursor="Hand"
                HorizontalAlignment="Center" Click="BtnCancel_Click"/>
    </Grid>
</Window>

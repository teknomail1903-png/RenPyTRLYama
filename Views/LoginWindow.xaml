<Window x:Class="RenPyTRLauncher.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TEKNO MARKET — Giriş"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#0A0A0C"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#9B59FF"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#8E44FF"/>
        <SolidColorBrush x:Key="CardBrush" Color="#1A1A22"/>
        <SolidColorBrush x:Key="InputBrush" Color="#25252D"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="#3A3A45"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A0A0A8"/>
        <SolidColorBrush x:Key="TextMuted" Color="#6A6A75"/>

        <DropShadowEffect x:Key="WindowShadow" BlurRadius="30" ShadowDepth="0" Opacity="0.4" Color="#000"/>
        <DropShadowEffect x:Key="ButtonShadow" BlurRadius="12" ShadowDepth="0" Opacity="0.3" Color="#9B59FF"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource InputBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Effect" Value="{StaticResource ButtonShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                x:Name="Border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Effect" Value="{x:Null}"/>
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                x:Name="Border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowControlBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                x:Name="Border"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2A2A35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="LinkBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"
                                   TextDecorations="Underline"
                                   Foreground="{TemplateBinding Foreground}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource CardBrush}"
            CornerRadius="12"
            Effect="{StaticResource WindowShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Window Controls -->
            <Grid Grid.Row="0" Height="36" Margin="0,6,6,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnMinimize" Content="─" Style="{StaticResource WindowControlBtn}" Click="BtnMinimize_Click"/>
                    <Button x:Name="BtnMaximize" Content="□" Style="{StaticResource WindowControlBtn}" Click="BtnMaximize_Click"/>
                    <Button x:Name="BtnClose" Content="✕" Style="{StaticResource WindowControlBtn}" Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

            <!-- Left Panel - Brand/Marketing -->
            <Border Grid.Column="0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#2D1B4E" Offset="0"/>
                        <GradientStop Color="#1A1A2E" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Margin="50,30" VerticalAlignment="Center">
                    <TextBlock Text="TEKNO" FontSize="42" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="MARKET" FontSize="42" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,-6,0,30"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource AccentBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="Oyun Yamaları" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource AccentBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="Otomatik Güncelleme" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource AccentBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="Hızlı İndirme" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource AccentBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="Güvenli Sistem" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Right Panel - Login Form -->
            <Border Grid.Column="1" Background="#1A1A22">
                <StackPanel Margin="40,40,40,40">
                        <TextBlock Text="Giriş Yap" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                        <TextBlock Text="Hesabınıza erişmek için giriş yapın" FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,0,0,28"/>

                        <TabControl x:Name="AuthTabs" Background="Transparent" BorderThickness="0">
                            <TabItem Header="Giriş">
                                <StackPanel Margin="0,16,0,0">
                                    <TextBlock Text="Kullanıcı Adı" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                    <TextBox x:Name="TxtLoginUser" TabIndex="1" KeyDown="TxtLoginUser_KeyDown"/>

                                    <TextBlock Text="Şifre" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                    <PasswordBox x:Name="TxtLoginPass" TabIndex="2" KeyDown="TxtLoginPass_KeyDown"/>

                                    <Grid Margin="0,0,0,20">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox x:Name="ChkRemember" Content="Beni Hatırla" Grid.Column="0" TabIndex="3"/>
                                        <Button x:Name="BtnForgotPassword" Content="Şifremi Unuttum" Style="{StaticResource LinkBtn}" Grid.Column="1" HorizontalAlignment="Right" Click="BtnForgotPassword_Click" TabIndex="4"/>
                                    </Grid>

                                    <Button x:Name="BtnLogin" Content="Giriş Yap" Style="{StaticResource PrimaryBtn}" Click="BtnLogin_Click" Margin="0,0,0,12" TabIndex="5"/>
                                    <Button x:Name="BtnRegister" Content="Hesap Oluştur" Style="{StaticResource SecondaryBtn}" Click="BtnRegister_Click" TabIndex="6"/>
                                </StackPanel>
                            </TabItem>

                            <TabItem Header="Kayıt">
                                <StackPanel Margin="0,16,0,0">
                                    <TextBlock Text="Kullanıcı Adı" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                    <TextBox x:Name="TxtRegUser" TabIndex="7"/>

                                    <TextBlock Text="E-posta" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                    <TextBox x:Name="TxtRegEmail" TabIndex="8"/>

                                    <TextBlock Text="Şifre" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                    <PasswordBox x:Name="TxtRegPass" TabIndex="9"/>

                                    <TextBlock Text="Şifre Tekrar" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                    <PasswordBox x:Name="TxtRegPass2" TabIndex="10"/>

                                    <Button x:Name="BtnRegisterMain" Content="HESAP OLUŞTUR" Style="{StaticResource PrimaryBtn}" Margin="0,20,0,0" Click="BtnRegister_Click" TabIndex="11"/>
                                </StackPanel>
                            </TabItem>
                        </TabControl>

                        <TextBlock x:Name="TxtError" Foreground="#E74C3C" FontSize="12" TextWrapping="Wrap"
                                   HorizontalAlignment="Center" Margin="0,16,0,0" Visibility="Collapsed"/>

                        <!-- Security & Info Area -->
                        <Border Background="#25252D" CornerRadius="6" Padding="14" Margin="0,20,0,0">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                                    <TextBlock Text="🔒" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="SSL Güvenli Giriş" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock Text="  •  " FontSize="11" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="️" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="Güvenli Sunucu" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                                    <TextBlock Text="🔄" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="Otomatik Güncelleme" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock Text="  •  " FontSize="11" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="🔐" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="Korumalı Hesap" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="v1.0.0" FontSize="10" Foreground="{StaticResource TextMuted}" Grid.Column="0" HorizontalAlignment="Center"/>
                                    <TextBlock Text="● Online" FontSize="10" Foreground="#4CAF50" Grid.Column="1" HorizontalAlignment="Center"/>
                                    <TextBlock Text="© 2026" FontSize="10" Foreground="{StaticResource TextMuted}" Grid.Column="2" HorizontalAlignment="Center"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
            </Border>
            </Grid>
        </Grid>
    </Border>
</Window>

<Window x:Class="RenPyTRLauncher.Views.ModernLauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RenPyTRLauncher.Data"
        Title="RenPy TR Launcher"
        Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#0A0E1A"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <local:SafeImageConverter x:Key="SafeImage"/>
        <SolidColorBrush x:Key="PrimaryAccent" Color="#6C5CE7"/>
        <SolidColorBrush x:Key="SecondaryAccent" Color="#00CEC9"/>
        <SolidColorBrush x:Key="DarkBackground" Color="#0A0E1A"/>
        <SolidColorBrush x:Key="CardBackground" Color="#1A1F2E"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A0A5B8"/>
        <SolidColorBrush x:Key="TextMuted" Color="#6B7280"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#10B981"/>
        <SolidColorBrush x:Key="WarningOrange" Color="#F59E0B"/>
        <SolidColorBrush x:Key="ErrorRed" Color="#EF4444"/>

        <DropShadowEffect x:Key="CardShadow" BlurRadius="12" ShadowDepth="0" Opacity="0.15" Color="#000"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0F1420"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="#2D3748"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="42"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#0F1420"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="#2D3748"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="42"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                x:Name="Border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#5A4FCF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2D3748"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                x:Name="Border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1A1F2E"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowControlBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                x:Name="Border"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1A1F2E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallGameCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,12,12"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.05" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.05" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryAccent}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"
                                   TextDecorations="Underline"
                                   Foreground="{TemplateBinding Foreground}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#00E5E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Border Grid.Row="0" Background="#0A0E1A" BorderBrush="#1A1F2E" BorderThickness="0,0,0,1" Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="RENPY TR" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,16,0"/>
                    <Ellipse Width="6" Height="6" Fill="{StaticResource SuccessGreen}" Margin="0,0,6,0"/>
                    <TextBlock Text="Online" Foreground="{StaticResource SuccessGreen}" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="BtnMinimize" Content="─" Style="{StaticResource WindowControlBtn}" Click="BtnMinimize_Click"/>
                    <Button x:Name="BtnMaximize" Content="□" Style="{StaticResource WindowControlBtn}" Click="BtnMaximize_Click"/>
                    <Button x:Name="BtnClose" Content="✕" Style="{StaticResource WindowControlBtn}" Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="680"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side -->
            <Grid Grid.Column="0" Background="#0D1117">
                <Grid.RowDefinitions>
                    <RowDefinition Height="340"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Hero Banner Carousel -->
                <Border Grid.Row="0" Margin="16,16,8,12" Height="340">
                    <Grid>
                        <Border x:Name="BannerContainer" Background="#1A1F2E" CornerRadius="8">
                            <Grid>
                                <Image x:Name="BannerImage" Stretch="UniformToFill" Opacity="0.6"/>
                                <Border>
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#1A1F2E" Offset="0"/>
                                            <GradientStop Color="#1A1F2E88" Offset="0.5"/>
                                            <GradientStop Color="#1A1F2E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                                <StackPanel VerticalAlignment="Bottom" Margin="20,20,20,20">
                                    <TextBlock x:Name="BannerTitle" Text="RenPy TR Launcher" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                    <TextBlock x:Name="BannerSubtitle" Text="En iyi Türkçe oyunlar burada" FontSize="14" Foreground="{StaticResource TextSecondary}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <!-- Carousel Indicators -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,16">
                            <Ellipse x:Name="IndicatorDot0" Width="8" Height="8" Fill="{StaticResource PrimaryAccent}" Margin="4,0"/>
                            <Ellipse x:Name="IndicatorDot1" Width="8" Height="8" Fill="#3D4659" Margin="4,0"/>
                            <Ellipse x:Name="IndicatorDot2" Width="8" Height="8" Fill="#3D4659" Margin="4,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Popular Games -->
                <Grid Grid.Row="1" Margin="16,8,8,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="🔥 Popüler Oyunlar" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="PopularGamesList">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource SmallGameCard}" MouseLeftButtonUp="GameCard_Click">
                                        <StackPanel>
                                            <Border Height="100" CornerRadius="8,8,0,0" Background="#2D3748" ClipToBounds="True">
                                                <Image Source="{Binding ImagePath, Converter={StaticResource SafeImage}}" Stretch="UniformToFill"/>
                                            </Border>
                                            <StackPanel Margin="8">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding TurkishStatusLabel}" Foreground="{Binding TurkishStatusColor}" FontSize="10" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Grid>

            <!-- Right Side - Login Panel -->
            <Border Grid.Column="1" Background="#0D1117" Margin="8,16,16,16">
                <Grid Margin="24,24,24,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="GİRİŞ YAP" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,24"/>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="E-posta" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtUsername" Margin="0,0,0,16"/>

                        <TextBlock Text="Şifre" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                        <Grid>
                            <PasswordBox x:Name="TxtPassword" Margin="0,0,0,12"/>
                            <TextBox x:Name="TxtPasswordVisible" Margin="0,0,0,12" Visibility="Collapsed"/>
                            <Button x:Name="BtnTogglePassword" Content="👁" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextMuted}" FontSize="14" Padding="8,0" HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand" Click="BtnTogglePassword_Click" Margin="0,0,12,12"/>
                        </Grid>

                        <CheckBox x:Name="ChkRemember" Content="Beni Hatırla" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,16"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <Button x:Name="BtnLogin" Content="Giriş Yap" Style="{StaticResource PrimaryButton}" Margin="0,0,0,12" Click="BtnLogin_Click"/>
                        <Button x:Name="BtnRegister" Content="Kayıt Ol" Style="{StaticResource SecondaryButton}" Margin="0,0,0,12" Click="BtnRegister_Click"/>
                        <Button x:Name="BtnForgotPassword" Content="Şifremi Unuttum" Style="{StaticResource LinkButton}" HorizontalAlignment="Center" Click="BtnForgotPassword_Click"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Margin="0,24,0,0">
                        <Border Height="1" Background="#1A1F2E" Margin="0,0,0,16"/>
                        
                        <!-- News/Announcements Section -->
                        <Border Background="#1A1F2E" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="📢 Duyurular" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                                <TextBlock x:Name="TxtNews" Text="Yeni oyunlar eklendi! VIP üyeliğe özel içerikler..." Foreground="{StaticResource TextSecondary}" FontSize="11" TextWrapping="Wrap" Height="40" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>

                        <Button x:Name="BtnVIP" Content="💎 VIP Üyelik" Background="#FFD700" Foreground="#000" BorderThickness="0" Padding="16,10" FontSize="13" FontWeight="SemiBold" Cursor="Hand" Height="40" Margin="0,0,0,8" Click="BtnVIP_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="BtnDiscord" Content="💬 Discord" Background="#5865F2" Foreground="White" BorderThickness="0" Padding="16,10" FontSize="13" FontWeight="SemiBold" Cursor="Hand" Height="40" Margin="0,0,0,8" Click="BtnDiscord_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Bottom Info Bar -->
        <Border Grid.Row="2" Background="#0A0E1A" BorderBrush="#1A1F2E" BorderThickness="0,1,0,0" Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBlock Text="🎮" FontSize="14" Margin="0,0,6,0"/>
                    <TextBlock Text="127" Foreground="{StaticResource TextPrimary}" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text=" Oyun" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,4"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBlock Text="🇹🇷" FontSize="14" Margin="0,0,6,0"/>
                    <TextBlock Text="89" Foreground="{StaticResource SuccessGreen}" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text=" Yama" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,4"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBlock Text="📥" FontSize="14" Margin="0,0,6,0"/>
                    <TextBlock Text="2.4M" Foreground="{StaticResource SecondaryAccent}" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text=" İndirme" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,4"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🟢" FontSize="14" Margin="0,0,6,0"/>
                    <TextBlock Text="Stabil" Foreground="{StaticResource SuccessGreen}" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text=" Sunucu" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
